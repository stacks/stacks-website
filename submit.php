<?php
if (isset($_POST["skip"])) {
  // get a new tag
  // = request an existing tag from the API and forward the user to a URL like <domain>/sloganerator/0123
}
elseif (isset($_POST["submit"])) {
  // submit the slogan
}
?>
